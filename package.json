{
  "name": "async-selector-kit-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "develop": "gatsby develop",
    "patch": "cp patches/404.mdx node_modules/smooth-doc/pages",
    "build": "yarn patch && gatsby build && cp CNAME public",
    "deploy": "gh-pages -d public -b gh-pages",
    "serve": "gatsby serve"
  },
  "dependencies": {
    "gatsby": "^2.15.18",
    "react": "^16.10.1",
    "react-dom": "^16.10.1",
    "smooth-doc": "^5.0.4"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
